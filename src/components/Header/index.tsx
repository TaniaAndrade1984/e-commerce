import React from 'react';

const Header: React.FC = () => {
  return (
    <header>
      <h1> Mafê Makeup</h1>
      {/* Adicione outros elementos do cabeçalho, como navegação, carrinho, etc. */}
    </header>
  );
};

export default Header;
